framework:
    cache:
        # Redis is used for the "app" pool (your custom TaskService and session data)
        app: cache.adapter.redis
        default_redis_provider: '%env(REDIS_URL)%'
        
        # APCu is used for the "system" pool (internal Symfony routing and framework configs)
        system: cache.adapter.apcu
